<style>
  #footer-container {
    background: var(--surface);
    border-top: 1px solid #E8E8E8;
    padding: var(--spacing-lg);
  }

  footer.wrapper {
    max-width: var(--container);
    margin: 0 auto;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--spacing-md);
    flex-wrap: wrap;
  }

  .footer-copy {
    font-size: 0.85rem;
    color: var(--secondary);
    margin: 0;
  }

  @media (max-width: 600px) {
    footer.wrapper {
      flex-direction: column;
      align-items: flex-start;
    }
  }
</style>

<div id="footer-container">
  <footer class="wrapper">
    <p class="footer-copy">&copy; SACsi — Soluciones Informáticas</p>
  </footer>
</div>
