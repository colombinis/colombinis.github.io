{
    "name": "sacsi-astro",
    "type": "module",
    "private": true,
    "scripts": {
        "dev": "astro dev --port 8000",
        "build": "astro build",
        "preview": "astro preview --port 8000",
        "test": "node --experimental-strip-types --test src/lib/rentabilidad.test.ts",
        "test:e2e": "playwright test",
        "test:e2e:install": "npm install --save-dev @playwright/test && npx playwright install --with-deps chromium"
    },
    "dependencies": {
        "@astrojs/sitemap": "^3.7.3",
        "astro": "^7.2.2"
    },
    "devDependencies": {
        "@playwright/test": "^1.49.0"
    }
}
